# Abyss Rogue index8 upload package

이 패키지는 기존 레포의 `index.html` 라우터를 건드리지 않고, 게임 본체를 `index8.html`로 올리는 용도입니다.

업로드 위치는 레포 루트입니다.

필수 파일:

- `index8.html`
- `asset_manifest.json`
- `assets/` 폴더 전체

기존 레포의 `index.html`은 유지하세요. 이미 `index8.html`로 우회/링크하는 구조라면 덮어쓰지 않아도 됩니다.
